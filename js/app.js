$(function() {

var contact_id = ci_session.getSessionData('contact_id');

if(contact_id > 0)
{
$('.notifications-menu').removeClass('hide');
}


// --------------------------------------------------------------------
});
